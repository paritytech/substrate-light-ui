{
  "name": "@polkadot/light-ui",
  "version": "0.1.0",
  "repository": "https://github.com/polkadot-js/light-ui.git",
  "description": "User interface optimized for the Substrate light client",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "tslint && tsc --noEmit",
    "test": "jest --coverage",
    "start": "lerna exec --scope @polkadot/light-apps yarn start"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.22.2",
    "eslint": "5.6.0"
  }
}
