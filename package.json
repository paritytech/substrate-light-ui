{
  "name": "@polkadot/light-ui",
  "repository": "https://github.com/polkadot-js/light-ui.git",
  "description": "User interface on top of Substrate light client",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "tslint --project . && tsc --noEmit",
    "test": "jest --coverage",
    "start": "cd packages/apps && webpack-serve --config webpack.config.js --port 3000"
  },
  "dependencies": {},
  "devDependencies": {
    "enzyme": "^3.6.0",
    "enzyme-adapter-react-16": "^1.7.0"
  }
}
